name: 📎 Attach binaries to release

on:
  release:
    types:
      - published

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      # TODO: dawidd6/action-download-artifact@v2
